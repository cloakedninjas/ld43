module LD43.State {
  export class Title extends Phaser.State {

    create() {
      /*setTimeout(function () {
        this.game.state.start('game', false);
      }.bind(this), 800);*/

      this.game.state.start('game', false);

      //  bg + instruction text

      // logo (from boot)

      // bag?

      // arrow

    }
  }
}
